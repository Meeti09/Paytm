# Frontend environment. Copy to .env.local.
#
# Only NEXT_PUBLIC_* values reach the browser, and this is the only one
# the frontend needs. Never put an API key in this file — the frontend
# has no secrets and calls no third-party service directly.

NEXT_PUBLIC_API_URL=http://localhost:8000
